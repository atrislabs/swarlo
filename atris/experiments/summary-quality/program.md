# Program

Improve `swarlo/sqlite_backend.py:summarize_for_member()` so open claims appear exactly once in the summary. Keep the main board context, keep the dedicated `OPEN CLAIMS` section, and remove the current duplicate rendering where an open claim shows up once in the main lines and again in the claim section.
