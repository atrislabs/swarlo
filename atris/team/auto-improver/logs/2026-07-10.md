## 18:29 · Mission started from run
- member: auto-improver
- mission: 6 hours self-improve Swarlo: each tick ships one real verified product gap (CLI/server/client/protocol), pytest must pass, commit and push, no empty receipts or busywork --self-drive
- cadence: 13m
- runner: claude
- lane: workspace
- verifier: python -m pytest -q --tb=line
## 18:30 · Mission started from run
- member: auto-improver
- mission: Self Drive 07 Mission Room with auto-improver: turn "--self-drive mission-2026-07-10-6-hours-self-improve-swarlo--678bcd0e" into one visible goal, task spine, proof receipt, and next action.
- cadence: 13m
- runner: codex_goal
- lane: workspace
- verifier: git diff --check

## Tick 1 · Closed client/server drift: SwarloClient.replay()
- gap: server exposed GET /api/{hub}/replay (late-joiner catch-up) but SwarloClient had no method — Python agents had to drop to raw HTTP
- fix: added `SwarloClient.replay(since, channel=None, limit=200)` returning posts oldest-first
- test: `test_replay_catches_up_posts` in tests/test_client.py (far-past since → all posts in order; far-future since → empty)
- doc: added `replay` to client method list in atris/MAP.md
- verifier: python -m pytest -q --tb=line → 438 passed (was 437)
- next: two remaining client gaps — server /prune and /members/{member_id} (single-member GET) still have no client method
## 18:33 · Mission run tick 1
- member: auto-improver
- mission: 6 hours self-improve Swarlo: each tick ships one real verified product gap (CLI/server/client/protocol), pytest must pass, commit and push, no empty receipts or busywork --self-drive
- state: ready
- tick_status: ran
- reason: tick-ok
- layer: capabilities
- verifier: passed
- receipt: atris/runs/mission-mission-2026-07-10-6-hours-self-improve-swarlo--678bcd0e-2026-07-11T01-33-35-402Z.json

## Tick 2 · Closed client/server drift: remove_member() + prune()
- gap: server exposed DELETE /members/{id} and POST /prune (member-lifecycle cleanup) but SwarloClient had no methods — orchestrators had to raw-HTTP to evict/prune stale agents
- fix: added `SwarloClient.remove_member(member_id)` and `SwarloClient.prune(stale_minutes=60)`
- tests: `test_remove_member` (delete one, confirm gone + keeper survives) and `test_prune_returns_shape` (fresh member not pruned) in tests/test_client.py
- gotcha: last join() overwrites the client's active api_key; deleting that identity 401s subsequent calls — join goner first so client stays keeper
- doc: added both to client method list in atris/MAP.md
- verifier: python -m pytest -q --tb=line → 440 passed (was 438)
- next: client-surface drift now closed for replay/remove_member/prune. Next look for CLI gaps or protocol edge cases (e.g. replay/prune not exposed as CLI subcommands).
## 18:48 · Mission run tick 2
- member: auto-improver
- mission: 6 hours self-improve Swarlo: each tick ships one real verified product gap (CLI/server/client/protocol), pytest must pass, commit and push, no empty receipts or busywork --self-drive
- state: ready
- tick_status: ran
- reason: tick-ok
- layer: capabilities
- verifier: passed
- receipt: atris/runs/mission-mission-2026-07-10-6-hours-self-improve-swarlo--678bcd0e-2026-07-11T01-48-30-511Z.json

## Tick 3 · Exposed replay on the CLI
- gap: server + client had replay (late-joiner catch-up) but no CLI subcommand — a human operator couldn't catch up on missed posts from the terminal
- fix: added `swarlo replay <since> [--channel] [--limit]` in swarlo/__main__.py (parser + handler, reuses _print_posts, clamps limit, prints "No posts since ..." when empty)
- tests: `test_replay_prints_posts` (asserts exact encoded URL) and `test_replay_empty_prints_notice` in tests/test_cli.py
- gotcha: _print_posts requires a `kind` key; real server posts always carry it, test fixture had to include it
- doc: added `replay` to CLI list in atris/MAP.md
- verifier: python -m pytest -q --tb=line → 442 passed (was 440)
- next: prune still has no CLI subcommand — it mutates members, so give it its own tick (POST /prune, --stale-minutes, print pruned list).
## 19:03 · Mission run tick 3
- member: auto-improver
- mission: 6 hours self-improve Swarlo: each tick ships one real verified product gap (CLI/server/client/protocol), pytest must pass, commit and push, no empty receipts or busywork --self-drive
- state: ready
- tick_status: ran
- reason: tick-ok
- layer: capabilities
- verifier: passed
- receipt: atris/runs/mission-mission-2026-07-10-6-hours-self-improve-swarlo--678bcd0e-2026-07-11T02-03-39-717Z.json

## Tick 4 · Exposed prune on the CLI
- gap: server + client had prune (evict non-human members not seen in N minutes) but no CLI subcommand — an operator couldn't clean up dead agents from the terminal
- fix: added `swarlo prune [--stale-minutes N]` in swarlo/__main__.py; POST /prune, clamps stale-minutes>=1, prints each removed member id and count, or "nothing pruned" when clean. Help text flags "(deletes members)".
- tests: `test_prune_lists_removed_members` (asserts POST url + payload, prints ids) and `test_prune_empty_prints_notice` in tests/test_cli.py
- doc: added `prune` to CLI list in atris/MAP.md
- verifier: python -m pytest -q --tb=line → 444 passed (was 442)
- milestone: client/server/CLI drift now fully closed for replay, remove_member, prune. Every server route reachable from client is reachable from CLI.
- next: drift-closing is done. Next look for a real protocol/UX gap — e.g. remove_member has no CLI command either (only prune bulk-removes); or inspect error handling / validation edge cases on an existing endpoint.
## 19:18 · Mission run tick 4
- member: auto-improver
- mission: 6 hours self-improve Swarlo: each tick ships one real verified product gap (CLI/server/client/protocol), pytest must pass, commit and push, no empty receipts or busywork --self-drive
- state: ready
- tick_status: ran
- reason: tick-ok
- layer: capabilities
- verifier: passed
- receipt: atris/runs/mission-mission-2026-07-10-6-hours-self-improve-swarlo--678bcd0e-2026-07-11T02-18-17-003Z.json

## Tick 5 · Exposed targeted member removal on the CLI
- gap: operators could bulk-prune stale agents from the terminal but couldn't evict one specific member by id (client had remove_member, CLI didn't)
- fix: added `swarlo remove <member_id>` in swarlo/__main__.py; DELETE /members/{id}, url-quotes the id, prints "Removed member: <id>", surfaces 404 via _raise_http_failure. Help flags "(deletes the member)".
- tests: `test_remove_member_prints_confirmation` (asserts DELETE url with quoted id) and `test_remove_missing_member_errors` (404 → SystemExit) in tests/test_cli.py
- doc: added `remove` to CLI list in atris/MAP.md
- verifier: python -m pytest -q --tb=line → 446 passed (was 444)
- next: member lifecycle fully covered at CLI. Look for a genuine protocol/validation gap next — e.g. inspect whether report/claim reject unknown task_key or bad status, or whether replay validates the since timestamp format server-side.
## 19:32 · Mission run tick 5
- member: auto-improver
- mission: 6 hours self-improve Swarlo: each tick ships one real verified product gap (CLI/server/client/protocol), pytest must pass, commit and push, no empty receipts or busywork --self-drive
- state: ready
- tick_status: ran
- reason: tick-ok
- layer: capabilities
- verifier: passed
- receipt: atris/runs/mission-mission-2026-07-10-6-hours-self-improve-swarlo--678bcd0e-2026-07-11T02-32-47-403Z.json

## Tick 6 · Hardened replay: reject malformed since instead of silent full-window
- gap: replay's since-clamp helper caught unparseable timestamps and silently fell back to the max 7-day lookback — a garbage `since` returned a whole window of posts as if the filter worked (silent wrong result). Valid-but-too-old correctly clamps; malformed should error.
- fix: server.py replay_posts now parses `since` up front and raises 400 with a clear ISO8601 hint on ValueError; the too-old clamping path is unchanged. _bounded_replay_since keeps its defensive fallback.
- tests: `test_replay_rejects_malformed_since` (400 + ISO8601 in detail) and `test_replay_clamps_valid_but_old_since` (epoch still 200) in tests/test_api.py
- verifier: python -m pytest -q --tb=line → 448 passed (was 446)
- note: CLI replay already routes non-200 through _raise_http_failure, so operators now see the 400 message instead of a misleading dump.
- next: continue validation hardening — check report status values and claim task_key on empty/whitespace, or whether prune/idle accept negative/huge minute args.
## 19:47 · Mission run tick 6
- member: auto-improver
- mission: 6 hours self-improve Swarlo: each tick ships one real verified product gap (CLI/server/client/protocol), pytest must pass, commit and push, no empty receipts or busywork --self-drive
- state: ready
- tick_status: ran
- reason: tick-ok
- layer: capabilities
- verifier: passed
- receipt: atris/runs/mission-mission-2026-07-10-6-hours-self-improve-swarlo--678bcd0e-2026-07-11T02-47-27-705Z.json

## Tick 7 · Hardened task_key: reject empty/whitespace, normalize padding
- gap: ClaimRequest/ReportRequest/AssignRequest declared task_key as bare str with no min length. An empty or whitespace-only task_key was accepted, creating a phantom shared lock (every ""-key claim collides as the same task); and "task:foo " vs "task:foo" forked into two separate locks.
- fix: added a shared `TaskKey = Annotated[str, StringConstraints(strip_whitespace=True, min_length=1)]` type in server.py and applied it to all three request models. Whitespace-only now 422s; padded keys normalize to the same lock.
- tests: test_claim_rejects_empty_task_key (empty/spaces/tabs → 422), test_claim_strips_task_key_whitespace (padded key collides 409, not a new claim), test_report_rejects_empty_task_key in tests/test_api.py
- verifier: python -m pytest -q --tb=line → 451 passed (was 448); no regressions from stripping
- next: continue validation hardening — assign has the same TaskKey now, but check assignee_id (can you assign to a nonexistent member?), or bound prune/idle minute args against negatives.
## 20:02 · Mission run tick 7
- member: auto-improver
- mission: 6 hours self-improve Swarlo: each tick ships one real verified product gap (CLI/server/client/protocol), pytest must pass, commit and push, no empty receipts or busywork --self-drive
- state: ready
- tick_status: ran
- reason: tick-ok
- layer: capabilities
- verifier: passed
- receipt: atris/runs/mission-mission-2026-07-10-6-hours-self-improve-swarlo--678bcd0e-2026-07-11T03-02-28-895Z.json

## Tick 8 · Hardened prune: clamp stale_minutes so it can't wipe the roster
- checked assign(assignee_id) first: backend already validates unknown assignee → 400, no gap there.
- gap: server /prune used stale_minutes from the body unbounded. stale_minutes=0 → cutoff=now → prunes every active member; negative → cutoff in the future → deletes every member with a last_seen (full roster wipe); non-numeric → 500. CLI clamped, but the Python client and raw API did not, so the destructive path was reachable.
- fix: server clamps stale_minutes to max(1, int(...)) and returns 400 on non-numeric, in server.py prune_members.
- tests: test_prune_negative_window_does_not_wipe_roster (0 and -5 both prune nothing), test_prune_rejects_non_numeric_window (400) in tests/test_api.py
- verifier: python -m pytest -q --tb=line → 453 passed (was 451)
- next: continue hardening — check idle's idle_minutes for the same negative/non-numeric issue, or expire/retry stale_minutes bounds; also client.prune/expire could clamp client-side for a clean error.
## 20:17 · Mission run tick 8
- member: auto-improver
- mission: 6 hours self-improve Swarlo: each tick ships one real verified product gap (CLI/server/client/protocol), pytest must pass, commit and push, no empty receipts or busywork --self-drive
- state: ready
- tick_status: ran
- reason: tick-ok
- layer: capabilities
- verifier: passed
- receipt: atris/runs/mission-mission-2026-07-10-6-hours-self-improve-swarlo--678bcd0e-2026-07-11T03-17-23-230Z.json

## Tick 9 · Hardened claims/expire: clamp stale_minutes so it can't wipe fresh claims
- gap: /claims/expire read stale_minutes from the body unbounded — the exact analog of the prune bug. stale_minutes=0 → cutoff=now → expires every open claim; negative → cutoff in the future → force_expire_claims marks ALL open claims 'stale', including fresh, actively-heartbeating ones (in-flight work wipe); non-numeric → 500 inside timedelta. Reachable via raw API and client.expire.
- fix: expire_stale_claims in server.py now clamps stale_minutes to max(1, int(...)) and returns 400 on non-numeric, mirroring prune_members.
- tests: test_expire_negative_window_does_not_wipe_fresh_claims (0 and -5 expire nothing), test_expire_rejects_non_numeric_window (400) in tests/test_api.py TestExpireClaims
- verifier: python -m pytest -q --tb=line → 455 passed (was 453)
- next: continue hardening — check /claims/retry max_retries (negative/non-numeric) and whether the Python client.expire/retry clamp client-side for a clean error before the round trip.
## 20:32 · Mission run tick 9
- member: auto-improver
- mission: 6 hours self-improve Swarlo: each tick ships one real verified product gap (CLI/server/client/protocol), pytest must pass, commit and push, no empty receipts or busywork --self-drive
- state: ready
- tick_status: ran
- reason: tick-ok
- layer: capabilities
- verifier: passed
- receipt: atris/runs/mission-mission-2026-07-10-6-hours-self-improve-swarlo--678bcd0e-2026-07-11T03-32-14-878Z.json

## Tick 10 · Hardened claims/retry: reject non-numeric max_retries (silent cap bypass)
- gap: /claims/retry read max_retries from the body unbounded. The backend filters `retry_count < max_retries`; SQLite orders every INTEGER before any TEXT, so a string max_retries (e.g. "lots") makes that predicate TRUE for every failed task — silently re-queuing poison tasks that already blew through their retry cap (invisible wrong result, worse than a 500). Negative was benign (retries nothing) but confusing.
- fix: retry_failed_tasks in server.py now coerces max_retries via max(0, int(...)) and returns 400 on non-numeric. Verified the SQLite affinity bug empirically (rc < 'foo' matched all 3 rows) before fixing.
- tests: test_retry_rejects_non_numeric_max_retries (400), test_retry_negative_max_retries_retries_nothing (200, empty) in tests/test_api.py TestRetryClaims
- verifier: python -m pytest -q --tb=line → 457 passed (was 455)
- next: the destructive-input sweep across prune/expire/retry is now closed. Consider client-side clamping in SwarloClient.expire/retry/prune for a clean pre-flight error, or move to a fresh area — e.g. whether /score or /liveness stale_minutes query params accept negatives.
## 20:46 · Mission run tick 10
- member: auto-improver
- mission: 6 hours self-improve Swarlo: each tick ships one real verified product gap (CLI/server/client/protocol), pytest must pass, commit and push, no empty receipts or busywork --self-drive
- state: ready
- tick_status: ran
- reason: tick-ok
- layer: capabilities
- verifier: passed
- receipt: atris/runs/mission-mission-2026-07-10-6-hours-self-improve-swarlo--678bcd0e-2026-07-11T03-46-53-089Z.json

## Tick 11 · Floored read limits so a negative limit can't dump the whole table
- first checked the flagged item (liveness/idle negative windows) and REJECTED it as a fake gap: test_integration.py:467 proves stale_minutes=0 is an intended "sweep everything now" feature, so a negative window expiring-all is not a new destructive capability. Reverted a speculative clamp that broke 2 tests rather than ship it.
- real gap: three read endpoints capped limit with a bare min(limit, N) and no floor — GET posts (50), GET summary (50), GET git/commits (200). A negative limit reaches SQLite as `LIMIT -n`, which SQLite treats as UNBOUNDED, so ?limit=-1 returned the entire table past the cap (memory/DoS on a busy hub). Verified empirically (LIMIT -1 → all 100 rows). The codebase already uses the correct max(1, min(int(limit), N)) idiom at 4 other sites.
- fix: applied max(1, min(limit, N)) to all three sites in server.py (list_posts, get_summary, git_list_commits).
- tests: test_negative_limit_does_not_bypass_cap in tests/test_api.py TestPosts (3 posts, ?limit=-1 → count == 1, not 3).
- verifier: python -m pytest -q --tb=line → 458 passed (was 457)
- next: check whether the briefing/replay/scores endpoints (lines ~597, 1368) have unfloored limits too, or whether the CLI passes negative limits through; also SwarloClient read methods could validate limit client-side.
## 21:04 · Mission run tick 11
- member: auto-improver
- mission: 6 hours self-improve Swarlo: each tick ships one real verified product gap (CLI/server/client/protocol), pytest must pass, commit and push, no empty receipts or busywork --self-drive
- state: ready
- tick_status: ran
- reason: tick-ok
- layer: capabilities
- verifier: passed
- receipt: atris/runs/mission-mission-2026-07-10-6-hours-self-improve-swarlo--678bcd0e-2026-07-11T04-04-22-695Z.json

## Tick 12 · Floored briefing limit so a negative value can't invert top-N
- swept the remaining limit sites flagged last tick. _list_unclaimed_tasks (len(out) >= limit break) is safe — both HTTP callers already floor to max(1, min(int(limit), 500)). The scores/xp endpoints likewise floor.
- real gap: get_briefing applied body.limit as a PYTHON SLICE (scored[: body.limit]), and BriefingRequest.limit had no lower bound. A negative limit turned "top N" into a from-the-end slice — limit=-5 dropped the 5 LOWEST-scored posts and returned everything else, the opposite of what limit means. Not a full dump (SQL is fixed LIMIT 200) but a real wrong result reachable via the API.
- fix: top = scored[: max(1, body.limit)] in server.py get_briefing, with a comment.
- tests: test_briefing_negative_limit_does_not_invert_top_n in tests/test_api.py TestBriefing (3 equally-relevant posts, limit=-1 → count == 1, not 2).
- verifier: python -m pytest -q --tb=line → 459 passed (was 458)
- next: the destructive/limit-input sweep across server endpoints is essentially exhausted. Consider moving to a fresh area — e.g. client-side validation in SwarloClient read methods so callers get a clean error before the round trip, or examine the CLI arg parsing for the same negative/non-numeric holes.
## 21:19 · Mission run tick 12
- member: auto-improver
- mission: 6 hours self-improve Swarlo: each tick ships one real verified product gap (CLI/server/client/protocol), pytest must pass, commit and push, no empty receipts or busywork --self-drive
- state: ready
- tick_status: ran
- reason: tick-ok
- layer: capabilities
- verifier: passed
- receipt: atris/runs/mission-mission-2026-07-10-6-hours-self-improve-swarlo--678bcd0e-2026-07-11T04-19-31-554Z.json

## Tick 13 · Added 'swarlo assign' CLI subcommand (closed a core-verb CLI gap)
- shifted off the input-clamp sweep (server side exhausted). Compared server routes vs CLI subcommands vs client methods.
- gap: assign — the push-assign verb (give a task to another member, creating a claim on their behalf) — existed on the server (POST channels/{channel}/assign) and in SwarloClient.assign, but had NO CLI subcommand. An operator could claim/report/post from the CLI but could not assign work to another agent. That's a real coordination hole, not a clamp.
- fix: added `swarlo assign <channel> <task_key> <assignee_id> <content>` with --priority (0-5) and --depends-on (comma-separated) in __main__.py — parser + dispatch handler mirroring the claim command, incl. 409 conflict / non-2xx exit handling and channel URL-quoting.
- tests: test_assign_uses_saved_runtime (URL, payload incl. priority + depends_on parsing, output) and test_assign_conflict_exits (409 → SystemExit) in tests/test_cli.py. Also smoke-checked `python -m swarlo assign --help`.
- verifier: python -m pytest -q --tb=line → 461 passed (was 459)
- next: keep closing CLI/client parity — expire, retry, liveness, briefing, and touch are client/server capabilities with no CLI subcommand. touch (heartbeat a claim) is the highest-value next one since it keeps long-running claims from being force-expired.
## 21:35 · Mission run tick 13
- member: auto-improver
- mission: 6 hours self-improve Swarlo: each tick ships one real verified product gap (CLI/server/client/protocol), pytest must pass, commit and push, no empty receipts or busywork --self-drive
- state: ready
- tick_status: ran
- reason: tick-ok
- layer: capabilities
- verifier: passed
- receipt: atris/runs/mission-mission-2026-07-10-6-hours-self-improve-swarlo--678bcd0e-2026-07-11T04-35-36-493Z.json

## Tick 14 · Added 'swarlo touch' CLI subcommand (heartbeat a claim from the terminal)
- gap: touch — refresh an open claim's heartbeat so the liveness/expire sweeps don't force-expire it — existed on the server (POST channels/{channel}/touch) and in SwarloClient.touch, but had no CLI subcommand. A long-running operator task claimed from the CLI had no CLI way to keep its claim alive; it would be reaped as stale after 30 min.
- fix: added `swarlo touch <channel> <task_key>` in __main__.py — parser + dispatch handler mirroring claim, with 404 ("No open claim") and non-2xx exit handling and channel URL-quoting.
- tests: test_touch_uses_saved_runtime (URL, payload, output) and test_touch_missing_claim_exits (404 → SystemExit) in tests/test_cli.py.
- verifier: python -m pytest -q --tb=line → 463 passed (was 461)
- next: keep closing CLI/client parity — expire, retry, and liveness are still client/server-only. liveness (alive/dying/dead + orphaned claims) is the highest-value next since it's the operator's health view of the swarm; expire/retry are the claim-GC verbs.
## 21:49 · Mission run tick 14
- member: auto-improver
- mission: 6 hours self-improve Swarlo: each tick ships one real verified product gap (CLI/server/client/protocol), pytest must pass, commit and push, no empty receipts or busywork --self-drive
- state: ready
- tick_status: ran
- reason: tick-ok
- layer: capabilities
- verifier: passed
- receipt: atris/runs/mission-mission-2026-07-10-6-hours-self-improve-swarlo--678bcd0e-2026-07-11T04-49-58-263Z.json

## Tick 15 · Added 'swarlo liveness' CLI subcommand (operator swarm-health view)
- gap: liveness — the alive/dying/dead health view with orphaned-claim detection — existed on the server (GET /liveness) and in SwarloClient.liveness, but had no CLI subcommand. An operator driving from the terminal had `idle` (who's not producing) but no way to see who's gone dark or which claims are orphaned by dead agents — the swarm's core health signal.
- fix: added `swarlo liveness [--stale-minutes N] [--no-expire]` in __main__.py — parser + handler printing alive/dying/dead counts, dying/dead members with last_seen, orphaned claims, sweep-expired keys, and the server recommendation. Clamps stale_minutes to >=1 and maps --no-expire to auto_expire=false so operators can observe without triggering the GC sweep.
- tests: test_liveness_prints_health (URL carries stale_minutes + auto_expire=true, prints counts/DYING/ORPHAN) and test_liveness_no_expire_observes_only (auto_expire=false) in tests/test_cli.py. Smoke-checked `python -m swarlo liveness --help`.
- verifier: python -m pytest -q --tb=line → 465 passed (was 463)
- next: expire and retry are the last two claim-GC verbs still client/server-only. expire (force-expire stale claims) is the natural next CLI subcommand; the server already clamps its stale_minutes.
## 22:04 · Mission run tick 15
- member: auto-improver
- mission: 6 hours self-improve Swarlo: each tick ships one real verified product gap (CLI/server/client/protocol), pytest must pass, commit and push, no empty receipts or busywork --self-drive
- state: ready
- tick_status: ran
- reason: tick-ok
- layer: capabilities
- verifier: passed
- receipt: atris/runs/mission-mission-2026-07-10-6-hours-self-improve-swarlo--678bcd0e-2026-07-11T05-04-46-398Z.json

## Tick 16 · Added 'swarlo expire' CLI subcommand (force-expire stale claims from the terminal)
- gap: expire — force-expire open claims with no heartbeat older than a window — existed on the server (POST /claims/expire) and in SwarloClient.expire, but had no CLI subcommand. When an agent dies holding a claim, an operator had liveness to SEE the orphan but no CLI verb to free it for reclaim (short of the destructive prune/remove which delete the member).
- fix: added `swarlo expire [--stale-minutes N]` in __main__.py — parser + handler printing each freed task_key and a total count. Floors stale_minutes to >=1 client-side (mirrors the tick-9 server clamp) so a negative window can't be sent as a claim-wipe request.
- tests: test_expire_reports_freed_claims (URL, payload, EXPIRED lines + count) and test_expire_clamps_negative_stale_minutes (-5 → sent as 1) in tests/test_cli.py. Smoke-checked `python -m swarlo expire --help`.
- verifier: python -m pytest -q --tb=line → 467 passed (was 465)
- next: retry (re-queue failed tasks up to max_retries) is the last claim-GC verb still client/server-only — natural next CLI subcommand; server already coerces max_retries (tick 10). After that, CLI/client parity is essentially closed; consider briefing or a fresh protocol area.
## Tick 24 · Briefing rejects unknown scorers (fixed a silent-wrong-result bug)
- gap: FRESH area — left CLI/client parity behind. Real correctness bug in the briefing endpoint: _briefing.score() defaults ANY unrecognized scorer name to v2_tfidf (SCORERS.get(scorer, v2_tfidf)), but the /briefing response echoes body.scorer verbatim. So a typo ("tfdif") or a not-yet-built scorer ("attention") silently returned tfidf results while the response claimed the bogus scorer ran. The operator gets plausible-looking results attributed to a scorer that never executed — the worst kind of wrong, because nothing signals the mistake. The CLI briefing verb passes --scorer straight through, so terminal users hit it too.
- fix: added a guard in get_briefing (server.py) that rejects body.scorer not in _briefing.SCORERS with HTTP 400 listing the valid set (tfidf/regex/prf/prf_gated/random). Fails fast before scoring. Left _briefing.score()'s library-level fallback untouched (other callers may rely on it) — the fix lives at the endpoint boundary where the lie was being told.
- tests: test_briefing_rejects_unknown_scorer ("tfdif" -> 400, detail names both the bad input and a valid option) and test_briefing_accepts_known_scorers (all 5 implemented scorers -> 200, response scorer matches request) in tests/test_api.py. Confirmed no existing test relied on the silent fallback before changing behavior.
- verifier: python -m pytest -q --tb=line → 491 passed (baseline 489 before this tick)
- next: candidate fresh gaps — (a) the suggest engine hardcodes only 4 channel names (general/experiments/ops/outreach) for its quiet-channel check, so custom channels never get reactivation suggestions; (b) audit other endpoints that echo a request field back without validating it (same class of bug). (a) is a concrete capability gap worth a tick.

## Tick 23 · Added reply/replies to the CLI + client.replies() (threaded discussion reaches the terminal)
- gap: compound. The server has both POST and GET on /posts/{id}/replies, but the client only wrapped the write (reply), never the read — and the CLI had neither. So threaded discussion (reply to a specific post, read a post's thread) was invisible from the terminal, and a client user couldn't read a thread back without hand-rolling HTTP.
- fix: (1) added client.replies(post_id) -> list[dict] (GET, oldest first, strips the envelope). (2) added `swarlo reply <post_id> <content>` (POST) and `swarlo replies <post_id>` (GET, prints "[time] who: content" per line, "No replies." when empty) in __main__.py. Closes the last client method with no CLI verb AND the client's own read-side reply gap in one increment.
- tests: CLI — test_reply_posts_to_thread (URL+payload+line), test_replies_lists_thread (2 replies, name + member_id fallback), test_replies_empty. Client — extended test_reply to post two replies and assert replies() reads them back oldest-first, plus test_replies_empty_for_unknown_post (unknown id -> []). All in tests/test_cli.py + tests/test_client.py. Smoke-checked both --help.
- verifier: python -m pytest -q --tb=line → 489 passed (baseline 485 before this tick)
- next: CLI now covers every client method that makes sense as a verb (push/fetch stay client-only by design; wait_for is a blocking helper). Parity work is essentially exhausted. Next tick should move to a genuinely new area — the suggest engine, briefing scorers, or a server/protocol capability — not more surface mirroring.

## Tick 22 · Added claim-file / file-claims CLI verbs (file-locking primitive reaches the terminal)
- gap: fresh area after git parity closed. Swarlo has a file-locking primitive — claim a file path so two agents don't edit it at once (POST /channels/{ch}/claim-file, GET /file-claims), with client wrappers claim_file/file_claims. But it had ZERO CLI presence: an operator or shell-driven agent could not lock a file or see which files were locked. This is a real coordination capability that was terminal-invisible.
- fix: added two verbs in __main__.py — `swarlo claim-file <channel> <file_path> [--content]` (POSTs the lock; on 409 exits naming the current holder from member_name/member_id; mirrors the existing `claim` verb's conflict handling) and `swarlo file-claims` (lists every locked file: path, who holds it, channel, since when; "No files claimed." when empty).
- tests: test_claim_file_locks_a_path (URL + payload + success line), test_claim_file_conflict_names_holder (409 -> SystemExit "already claimed by Bob"), test_file_claims_lists_locked_files (prints path/holder/channel), test_file_claims_empty in tests/test_cli.py. Smoke-checked both --help.
- verifier: python -m pytest -q --tb=line → 485 passed (baseline 481 before this tick)
- next: `reply` is the other client method with no CLI verb (reply to a post by id) — small, clean follow-up. Or move further afield: the suggest engine, or briefing output. file-locking is now complete on the CLI.

## Tick 21 · Added 'swarlo diff' CLI verb (git parity now closed on both client and CLI)
- gap: tick 20 shipped commits/leaves/lineage but deferred diff because the CLI's _request json.loads() every body and the diff endpoint returns plaintext. So an operator could list and trace commits from the terminal but couldn't compare two — the last read-side git verb missing from the shell.
- fix: added a _request_text helper in __main__.py (twin of the client's tick-19 _request_bytes — returns raw response text on success, still parses a JSON error detail so _raise_http_failure formats failures identically) and the `swarlo diff <hash_a> <hash_b>` verb on top of it. Prints the plaintext diff as-is (no double newline), or "(no differences)" when the bodies match. That closes git parity: all read-side git verbs (commits/leaves/lineage/diff) now exist on the CLI, matching the full client surface from ticks 18-19.
- tests: test_diff_prints_plaintext (URL ends /git/diff/aaa111/bbb222, prints -hello/+world) and test_diff_empty_reports_no_differences (blank body -> "(no differences)") in tests/test_cli.py, both faking _request_text. Smoke-checked `swarlo diff --help`.
- verifier: python -m pytest -q --tb=line → 481 passed (baseline 479 before this tick)
- next: git parity is fully closed (client + CLI, read + binary). Push/fetch are client-only by design (binary bundle transport doesn't belong in a print-oriented CLI). Move to a FRESH area next tick — candidates: briefing output polish, a server/protocol gap, or the suggest engine. Stop mirroring; find a new product gap.

## Tick 20 · Added commits/leaves/lineage CLI verbs (git graph now visible from the terminal)
- gap: after ticks 18-19 the git commit DAG was fully reachable from the Python client, but an operator at the terminal still had no way to see it — no `swarlo commits`, no leaves, no lineage. The parity theme extends to the CLI: operators drive Swarlo from the shell and had zero visibility into the shared commit graph.
- fix: added 3 read-side git verbs in __main__.py — `swarlo commits [--member M] [--limit N]` (newest-first list, clamps limit 1..200, maps --member to member_filter), `swarlo leaves` (DAG tips), `swarlo lineage <hash>` (ancestor chain). One shared handler builds the URL/route/label per verb and prints a `<short-hash> <member> <first-line-of-message>` line for each commit. Deferred `swarlo diff <a> <b>`: the CLI's _request json.loads() the body, and diff returns plaintext — same non-JSON-transport gap I deferred on the client in tick 19; it needs a raw-body path here too.
- tests: test_commits_lists_dag (URL carries limit+member_filter, prints 2 commits, 12-char short hash, only the first message line, falls back to member_id when no name), test_leaves_reports_empty (hits /git/leaves, empty message), test_lineage_walks_from_hash (URL ends /git/commits/<hash>/lineage) in tests/test_cli.py. Smoke-checked all three --help.
- verifier: python -m pytest -q --tb=line → 479 passed (baseline 476 before this tick; the prior log's 473 was a stale run count). Confirmed baseline by stashing my diff and re-running.
- next: `swarlo diff <a> <b>` — needs the CLI _request to handle a plaintext (non-JSON) response body, the terminal-side twin of tick 19's _request_bytes. After that, git parity is closed on both client and CLI; move to a fresh protocol area.

## Tick 19 · Added git push/fetch/diff to SwarloClient (binary DAG transport, client surface now complete)
- gap: tick 18 wrapped the 5 read-side git endpoints but deferred push/fetch/diff — the write/binary slice. The client's _request only spoke JSON, so an agent still couldn't share code (push a bundle), pull a peer's commit (fetch), or compare two commits (diff) without hand-rolling HTTP. This was the last hole in client/server git parity.
- fix: added a _request_bytes transport helper to client.py (sends/returns raw bytes, 60s timeout for up-to-50MB bundles, still decodes a JSON error detail on failure) and 3 methods on top of it — git_push(bundle)->{'hashes'}, git_fetch(hash)->bytes, git_diff(a,b)->str. All 8 git endpoints now have client wrappers (was 0 two ticks ago).
- tests: extended TestClientGitDAG in tests/test_client.py — test_git_push_fetch_diff_roundtrip builds a real 2-commit git bundle, pushes it, confirms the indexed tip is queryable, fetches a valid bundle back as bytes, and asserts the diff shows -hello/+world; test_git_fetch_missing_raises_404 checks the 404 path. Learned: unbundle/push returns only the *tip* hash in {'hashes'}, but both commit objects land in the DAG store — so diff/fetch work across commits the SQLite index never saw. Adjusted the test to match that real behavior rather than assume both hashes are indexed.
- verifier: python -m pytest -q --tb=line → 473 passed (was 471)
- next: git parity is closed at the client. Options: (a) CLI verbs over the git graph (swarlo commits/leaves/lineage/diff) so operators get it from the terminal too, or (b) move to a fresh protocol area. (a) is the natural continuation of the parity theme.

## Tick 18 · Added git-DAG read methods to SwarloClient (commit graph was server-only)
- gap: the server exposes a whole git commit-DAG subsystem — 8 endpoints under /git/* (push, fetch, commits, commit, children, leaves, lineage, diff) — but SwarloClient had ZERO wrappers for any of them. An agent using the Python client could push code but couldn't inspect the shared commit graph programmatically; it had to hand-roll HTTP. This was the biggest client/server parity gap left after the claim-GC verbs closed.
- fix: added the 5 read-side JSON methods to client.py — git_commits(member_filter, limit), git_commit(hash), git_children(hash), git_leaves(), git_lineage(hash). Each mirrors its endpoint; git_commits clamps limit to 1..200 (matches the server's max(1,min(limit,200))) and URL-quotes hashes. Deferred push/fetch/diff (binary bundle upload / octet-stream download / plaintext) as a separate slice — they need non-JSON transport the current _request doesn't do.
- tests: TestClientGitDAG in tests/test_client.py — test_git_dag_read_methods indexes a root→mid→tip chain plus a sibling leaf via the live server's backend, then asserts list/single/children/leaves/lineage all return the right shape; test_git_commit_missing_raises_404 checks the 404 path.
- verifier: python -m pytest -q --tb=line → 471 passed (was 469)
- next: the write/binary git slice — git_push (upload a bundle), git_fetch (download a bundle), git_diff (plaintext between two hashes). These need _request to handle non-JSON bodies/responses, so it's a client-transport change, not just a wrapper. After that, add CLI verbs over the git graph if useful.

## Tick 17 · Added 'swarlo retry' CLI subcommand (re-queue failed tasks from the terminal)
- gap: retry — re-open failed tasks for claiming up to max_retries — existed on the server (POST /claims/retry) and in SwarloClient.retry, but had no CLI subcommand. It was the last claim-GC verb still client/server-only: an operator could see failed tasks but had no terminal verb to re-queue the recoverable ones.
- fix: added `swarlo retry [--max-retries N]` in __main__.py — parser + handler printing each re-queued task_key (RETRY: ...) and a total count. Clamps max_retries to >=0 client-side, mirroring the tick-10 server max(0, ...) coercion so a negative/bogus cap can't be sent (a string/negative value can flip the SQLite retry_count<max_retries predicate for every failed task).
- tests: test_retry_reports_requeued_tasks (URL, payload, RETRY lines + count) and test_retry_clamps_negative_max_retries (-3 → sent as 0) in tests/test_cli.py. Smoke-checked `python -m swarlo retry --help`.
- verifier: python -m pytest -q --tb=line → 469 passed (was 467)
- next: CLI/client parity across the claim-GC verbs (touch/assign/liveness/expire/retry) is now closed. Next tick should look at a fresh area — briefing output, or a server/protocol gap — rather than more CLI mirroring.

## 22:19 · Mission run tick 16
- member: auto-improver
- mission: 6 hours self-improve Swarlo: each tick ships one real verified product gap (CLI/server/client/protocol), pytest must pass, commit and push, no empty receipts or busywork --self-drive
- state: ready
- tick_status: ran
- reason: tick-ok
- layer: capabilities
- verifier: passed
- receipt: atris/runs/mission-mission-2026-07-10-6-hours-self-improve-swarlo--678bcd0e-2026-07-11T05-19-28-818Z.json
## 22:34 · Mission run tick 17
- member: auto-improver
- mission: 6 hours self-improve Swarlo: each tick ships one real verified product gap (CLI/server/client/protocol), pytest must pass, commit and push, no empty receipts or busywork --self-drive
- state: ready
- tick_status: ran
- reason: tick-ok
- layer: capabilities
- verifier: passed
- receipt: atris/runs/mission-mission-2026-07-10-6-hours-self-improve-swarlo--678bcd0e-2026-07-11T05-34-52-094Z.json
## 22:51 · Mission run tick 18
- member: auto-improver
- mission: 6 hours self-improve Swarlo: each tick ships one real verified product gap (CLI/server/client/protocol), pytest must pass, commit and push, no empty receipts or busywork --self-drive
- state: ready
- tick_status: ran
- reason: tick-ok
- layer: capabilities
- verifier: passed
- receipt: atris/runs/mission-mission-2026-07-10-6-hours-self-improve-swarlo--678bcd0e-2026-07-11T05-51-04-080Z.json
## 23:07 · Mission run tick 19
- member: auto-improver
- mission: 6 hours self-improve Swarlo: each tick ships one real verified product gap (CLI/server/client/protocol), pytest must pass, commit and push, no empty receipts or busywork --self-drive
- state: ready
- tick_status: ran
- reason: tick-ok
- layer: capabilities
- verifier: passed
- receipt: atris/runs/mission-mission-2026-07-10-6-hours-self-improve-swarlo--678bcd0e-2026-07-11T06-07-03-911Z.json
## 23:23 · Mission run tick 20
- member: auto-improver
- mission: 6 hours self-improve Swarlo: each tick ships one real verified product gap (CLI/server/client/protocol), pytest must pass, commit and push, no empty receipts or busywork --self-drive
- state: ready
- tick_status: ran
- reason: tick-ok
- layer: capabilities
- verifier: passed
- receipt: atris/runs/mission-mission-2026-07-10-6-hours-self-improve-swarlo--678bcd0e-2026-07-11T06-23-33-510Z.json
## 23:38 · Mission run tick 21
- member: auto-improver
- mission: 6 hours self-improve Swarlo: each tick ships one real verified product gap (CLI/server/client/protocol), pytest must pass, commit and push, no empty receipts or busywork --self-drive
- state: ready
- tick_status: ran
- reason: tick-ok
- layer: capabilities
- verifier: passed
- receipt: atris/runs/mission-mission-2026-07-10-6-hours-self-improve-swarlo--678bcd0e-2026-07-11T06-38-16-973Z.json
## 23:54 · Mission run tick 22
- member: auto-improver
- mission: 6 hours self-improve Swarlo: each tick ships one real verified product gap (CLI/server/client/protocol), pytest must pass, commit and push, no empty receipts or busywork --self-drive
- state: ready
- tick_status: ran
- reason: tick-ok
- layer: capabilities
- verifier: passed
- receipt: atris/runs/mission-mission-2026-07-10-6-hours-self-improve-swarlo--678bcd0e-2026-07-11T06-54-07-296Z.json
